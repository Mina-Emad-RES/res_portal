export type Campaign = {
  id: string;
  name: string;
  driveFolderId: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
};
export type CampaignItem = {
  label: string;
  value: string;
};
